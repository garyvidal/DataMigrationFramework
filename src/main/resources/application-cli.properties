# CLI profile — disables the embedded web server so the JVM exits when the migration finishes.
spring.main.web-application-type=none

# Suppress Spring Boot banner and reduce startup noise
spring.main.banner-mode=off
logging.level.root=WARN
logging.level.com.nativelogix.data.migration.framework=INFO
logging.level.org.springframework.batch=WARN
